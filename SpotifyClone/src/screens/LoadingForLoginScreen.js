import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const LoadingForLoginScreen = () => {
  return (
    <View>
      <Text>Loading...</Text>
    </View>
  )
}

export default LoadingForLoginScreen

const styles = StyleSheet.create({})